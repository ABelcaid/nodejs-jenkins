pipeline {
  agent {dockerfile true}
    
  stages {

    stage('Build') {
      steps {
        echo 'building the software'
        echo 'building the software v2'
        sh 'npm i'
      }
    }  
    
  }
}